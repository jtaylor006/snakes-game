import pygame

pygame.init()
dis=pygame.display.set_mode((400,300))
pygame.display.update()

pygame.display.set_caption('Snake game by Jarred')
game_over=False
while not game_over:
    for even in pygame.event.get():
        print(event) #prints all of the actions that takes place

pygame.quit()
quit()